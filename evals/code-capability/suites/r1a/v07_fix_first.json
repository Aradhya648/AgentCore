{
  "id": "r1a_v07_fix_first",
  "category": "tool_use",
  "kind": "fix",
  "phase": "R1a",
  "vendor_id": "V07",
  "vendor_dir": "more-itertools@64be96ceb2a6",
  "language": "python",
  "user_message": "工作区是 more-itertools。`first()` 返回值异常（拿到的不是首元素）。请做最小修复，使 `python -m pytest tests/test_more.py::FirstTests -q` 通过。不要修改 tests/。",
  "path": "single",
  "mode": "economy",
  "toolset": "worker",
  "seed_patch": "seeds/v07_break_first.json",
  "protected_paths": ["tests"],
  "test_command": ["python", "-m", "pytest", "tests/test_more.py::FirstTests", "-q"],
  "checks": [
    {
      "name": "TestExitCode",
      "args": {
        "command": ["python", "-m", "pytest", "tests/test_more.py::FirstTests", "-q"],
        "expected_exit": 0,
        "timeout_sec": 120
      }
    },
    {
      "name": "TestsUnchanged",
      "args": { "paths": ["tests"] }
    }
  ]
}
