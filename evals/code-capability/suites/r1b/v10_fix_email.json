{
  "id": "r1b_v10_fix_email",
  "category": "tool_use",
  "kind": "fix",
  "phase": "R1b",
  "vendor_id": "V10",
  "vendor_dir": "zod@e30870369d5b",
  "language": "typescript",
  "user_message": "工作区是 zod（v3 pin）。`z.string().email()` 校验行为异常。请做最小修复。Windows 友好：先由 harness `npm install`，再 `node node_modules/jest/bin/jest.js --config ./configs/ts-jest.config.json src/__tests__/string.test.ts -t \"email validations\"`（ts-jest 直跑源码，勿依赖 bash）。不要修改 src/__tests__/。",
  "path": "team",
  "mode": "economy",
  "toolset": "ceo",
  "seed_patch": "seeds/v10_break_email.json",
  "protected_paths": ["src/__tests__"],
  "test_command": ["node", "node_modules/jest/bin/jest.js", "--config", "./configs/ts-jest.config.json", "src/__tests__/string.test.ts", "-t", "email validations"],
  "checks": [
    {
      "name": "TestExitCode",
      "args": {
        "command": ["node", "node_modules/jest/bin/jest.js", "--config", "./configs/ts-jest.config.json", "src/__tests__/string.test.ts", "-t", "email validations"],
        "expected_exit": 0,
        "timeout_sec": 300
      }
    },
    {
      "name": "TestsUnchanged",
      "args": { "paths": ["src/__tests__"] }
    }
  ]
}
