{
  "id": "r2_v04_extend_join_url_paths",
  "category": "tool_use",
  "kind": "extend",
  "phase": "R2",
  "vendor_id": "V04",
  "vendor_dir": "flask@22d924701a6a",
  "language": "python",
  "user_message": "工作区是 flask（src layout）。请新增公开 API `flask.join_url_paths(*parts: str) -> str`：用单斜杠拼接 URL 路径段（忽略空段、剥离首尾 /）；无有效段时返回 \"/\"。实现放在 `helpers.py` 并导出。不要修改既有 tests/；评测会追加 GOLDEN 测。",
  "path": "team",
  "mode": "economy",
  "toolset": "ceo",
  "golden_tests": [
    {
      "src": "golden/v04_join_url_paths_test.py",
      "dest": "tests/test_agentcore_extend_join_url_paths.py"
    }
  ],
  "reference_patch": "seeds/v04_extend_join_url_paths.json",
  "protected_paths": ["tests"],
  "test_command": ["python", "-m", "pytest", "tests/test_agentcore_extend_join_url_paths.py", "-q"],
  "checks": [
    {
      "name": "TestExitCode",
      "args": {
        "command": ["python", "-m", "pytest", "tests/test_agentcore_extend_join_url_paths.py", "-q"],
        "expected_exit": 0,
        "timeout_sec": 120,
        "pythonpath": ["src"]
      }
    },
    {
      "name": "TestsUnchanged",
      "args": {
        "paths": ["tests"],
        "allow_extra": ["tests/test_agentcore_extend_join_url_paths.py"]
      }
    }
  ]
}
