{
  "format": "replacements_v1",
  "description": "R2 Extend 参照实现：ZodString.hexColor() via regex",
  "replacements": [
    {
      "path": "src/types.ts",
      "old": "  ulid(message?: errorUtil.ErrMessage) {\n    return this._addCheck({ kind: \"ulid\", ...errorUtil.errToObj(message) });\n  }\n  base64(message?: errorUtil.ErrMessage) {\n",
      "new": "  ulid(message?: errorUtil.ErrMessage) {\n    return this._addCheck({ kind: \"ulid\", ...errorUtil.errToObj(message) });\n  }\n  /** AgentCore R2 Extend reference: CSS hex color */\n  hexColor(message?: errorUtil.ErrMessage) {\n    return this.regex(/^#([0-9a-fA-F]{3}|[0-9a-fA-F]{6})$/, message);\n  }\n  base64(message?: errorUtil.ErrMessage) {\n"
    }
  ]
}
