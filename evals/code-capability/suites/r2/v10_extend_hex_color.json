{
  "id": "r2_v10_extend_hex_color",
  "category": "tool_use",
  "kind": "extend",
  "phase": "R2",
  "vendor_id": "V10",
  "vendor_dir": "zod@e30870369d5b",
  "language": "typescript",
  "user_message": "工作区是 zod（v3 pin）。请为 `ZodString` 新增方法 `hexColor(message?)`：校验 CSS hex 颜色（#RGB 或 #RRGGBB）。可用现有 `regex`。Windows 友好：ts-jest 直跑源码。不要修改既有 src/__tests__/；评测追加 GOLDEN 测。",
  "path": "team",
  "mode": "economy",
  "toolset": "ceo",
  "golden_tests": [
    {
      "src": "golden/v10_hex_color_test.ts",
      "dest": "src/__tests__/agentcore_extend_hex_color.test.ts"
    }
  ],
  "reference_patch": "seeds/v10_extend_hex_color.json",
  "protected_paths": ["src/__tests__"],
  "test_command": ["node", "node_modules/jest/bin/jest.js", "--config", "./configs/ts-jest.config.json", "src/__tests__/agentcore_extend_hex_color.test.ts"],
  "checks": [
    {
      "name": "TestExitCode",
      "args": {
        "command": ["node", "node_modules/jest/bin/jest.js", "--config", "./configs/ts-jest.config.json", "src/__tests__/agentcore_extend_hex_color.test.ts"],
        "expected_exit": 0,
        "timeout_sec": 300
      }
    },
    {
      "name": "TestsUnchanged",
      "args": {
        "paths": ["src/__tests__"],
        "allow_extra": ["src/__tests__/agentcore_extend_hex_color.test.ts"]
      }
    }
  ]
}
