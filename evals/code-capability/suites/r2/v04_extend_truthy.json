{
  "id": "r2_v04_extend_truthy",
  "category": "tool_use",
  "kind": "extend",
  "phase": "R2",
  "vendor_id": "V04",
  "vendor_dir": "flask@22d924701a6a",
  "language": "python",
  "user_message": "工作区是 flask（src layout）。请新增公开 API `flask.truthy(value: str | None) -> bool`：配置/环境风格真值判断；None、空串、0/false/no/off/n（大小写不敏感）为 False，其余非空串为 True。实现放在 `src/flask/helpers.py` 并在 `__init__.py` 导出。不要修改既有 tests/；评测会追加 GOLDEN 测。",
  "path": "single",
  "mode": "economy",
  "toolset": "worker",
  "golden_tests": [
    {
      "src": "golden/v04_truthy_test.py",
      "dest": "tests/test_agentcore_extend_truthy.py"
    }
  ],
  "reference_patch": "seeds/v04_extend_truthy.json",
  "protected_paths": ["tests"],
  "test_command": ["python", "-m", "pytest", "tests/test_agentcore_extend_truthy.py", "-q"],
  "checks": [
    {
      "name": "TestExitCode",
      "args": {
        "command": ["python", "-m", "pytest", "tests/test_agentcore_extend_truthy.py", "-q"],
        "expected_exit": 0,
        "timeout_sec": 120,
        "pythonpath": ["src"]
      }
    },
    {
      "name": "TestsUnchanged",
      "args": {
        "paths": ["tests"],
        "allow_extra": ["tests/test_agentcore_extend_truthy.py"]
      }
    }
  ]
}
