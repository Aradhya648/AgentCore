# fix-me-kit 固定 Prompt（S2 / S6 对照同文）

当前工作区是一个故意有 Bug 的小 CLI（`python -m fixme`）。请：

1. 先读代码与测试，定位失败原因（不要盲改）。
2. 用**最小改动**让 `python -m pytest -q` 全绿。
3. 不要删测试、不要重写整个项目、不要改工作区外文件。

完成后列出修改过的文件，并说明每个 Bug 的根因一句话。
