# 生产构建烘焙的后端地址。Vite 在 production 构建期把 import.meta.env.VITE_API_URL
# 静态替换进产物；Pages 部署脚本也会注入同名变量（覆盖本文件）。非密钥、可入仓。
# 本机临时覆盖用 apps/mobile/.env.production.local（若需）。
VITE_API_URL=https://app.fashitianxia.xyz/api
