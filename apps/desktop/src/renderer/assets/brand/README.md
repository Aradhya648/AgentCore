# Desktop brand assets

- `fonts/` — Space Grotesk (SIL OFL 1.1); Latin cuts for brand wordmark only. See `fonts/OFL.txt`.
- Mark geometry lives in `components/brand/BrandMark.tsx` (inline SVG, `currentColor` / `text-primary`).
