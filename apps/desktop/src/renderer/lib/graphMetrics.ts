/**
 * Re-export shim — implementation lives in `@agentcore/graph-layout`.
 */
export {
  NODE_WIDTH,
  NODE_HEIGHT,
  LAYOUT_PADDING,
  buildNodeSizeMap,
  type NodeSizeEntry,
  type NodeSizeMap,
} from "@agentcore/graph-layout";
